{
  "name": "hms-sdk-impl",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "updateSDK": "rm -rf node_modules package-lock.json && npm cache clean --force && npm i"
  },
  "dependencies": {
    "hms-core-sdk": "github:ShivamTiwariCodes/hms-core-sdk"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
